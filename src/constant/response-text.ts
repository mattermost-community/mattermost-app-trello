import { h5 } from "../utils/markdown";

function subscriptionCreatedTitle(): string {
   return h5(`Mattermost Trello Plugin - Slash Command Help`);
}

function getCommands(): string {
   let text = ''//getUserCommands();
   return text;
}